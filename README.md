This is a angular and node.js based project used for datavisualization. 
Jsondata is stored in mongodb database which will be retreived from db and displayed using angular.
Used Angular material for UI.
